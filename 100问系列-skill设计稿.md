# 100q-write · 技能设计稿（v1，待安装）

> 这是「100 问系列内容创作流水线」的技能化设计。当前为**设计稿**，未安装。
> 审阅通过后，将拆为 `~/.workbuddy/skills/100q-write/` 下的 SKILL.md + contract-template.md + gate-checklist.md + references/pipeline-overview.md。

## 1. 技能定位
编排型 **conductor**：不重写底层能力，按 4 阶段推进 100 问书籍创作，强制铁律与闸门，内建写校改螺旋。主线 = 规划 → 撰写 → 审校 → 延展；纯内容创作（资料搜集与 git 已剔除）。

## 2. Frontmatter
```yaml
name: 100q-write
description: 100 问系列书籍内容创作编排流水线。触发："写一本100问""100问创作""100问流水线""开始写100问"。编排 规划→撰写→审校→延展 四阶段，强制 零号铁律+五禁令 与 G1–G4 闸门，写校改螺旋闭环。依赖 dbs / airay-100-questions / dbs-content / dbs-ai-check。
version: 1.0.0
type: orchestration
human_in_loop: true
triggers:
  - "写一本100问"
  - "100问创作"
  - "100问流水线"
  - "开始写100问"
```

## 3. 何时使用 / 何时不用
- **用**：从 0 写一本 100 问书（中文定稿为主线，英文版可选）。
- **不用**：单篇短文、非 100 问结构的书、纯翻译。

## 4. 前置依赖（必须已安装）
| 技能 | 用在 | 版本约定 |
|---|---|---|
| dbs | A1 画像与需求 | 商业工具箱主入口 |
| airay-100-questions | A2 定 mode B | v2.0 双模式 |
| dbs-content | A4 写作方向诊断 | dontbesilent 内容诊断 |
| dbs-ai-check | C1 质检门禁 | 质检门禁 |

> 依赖接口若改名/改参，本技能需同步更新「节点 → 技能」映射。

## 5. 核心不变量（全书生效，不可被阶段跳过）
1. **零号铁律**：禁杜撰案例与无来源数字。
2. **五禁令**：禁破折号 `——` / 禁 `不是…而是…` / 禁正文露信源 / 禁 AI 总结腔（"本质上""更准确地说"）/ 禁模板腔（"值得注意的是"）。
3. **确认闭环**：改正文前先让你拍板；D1 先出「新料嵌入对照表」不直接改，只应用你选定的，被拒即丢。

## 6. 四阶段编排

### 阶段 1 · 规划定位（A）—— 动笔前
1. **A1 用户画像与需求分析** → 调 `/dbs`，定目标读者 + 痛点/痒点/卡点 + 出圈双引擎（向往+恐惧）。画像具体可落地，不写泛泛"技术人"。
2. **A2 选题定调与模式选择** → 调 `/airay-100-questions` 定 mode B（钩子书）；定全书暗线与出圈定位。方向定调不轻易推翻；结构/模板/引用不碰。
3. **A3 写 100 问总纲** → 拆 100 问落 N 章，产出 `00-100问总纲.md`（题号+钩子写法）。题号全书连续 1–100；H1 带序号；章首引子绝不用编辑腔标签。
4. **A4 写作方向诊断** → 调 `/dbs-content` 定"答疑+交付体"四段式（钩子→认知→利他动作→资源块）；高密度零废话；利他≠说明书；内联 ≤15 行短资源。
- **产出**：`00-总纲` + 写作契约（从 `contract-template.md` 实例化）。
- **闸门 G1**：你确认总纲与契约锁定，方可开写。未锁定不得进入阶段 2。

### 阶段 2 · 撰写（B）—— 核心生产
1. **B1 逐章逐题成稿**：按总纲写 100 问；口语风，现象→原因→当下→短判断；把评估过的资料**顺译**集成进对应题；可复制模板用 `reference` 格式内联（不计入字数）。
2. **B2 章首引子与去编辑腔**：每章开头写引子钩子段；自然段落流，去段标；不写结尾总结句。
- **产出**：100 问草稿（zh）。
- **闸门 G2**：100 题齐 + 资料无遗漏，方可审校。漏题/漏料回 B1 补。

### 阶段 3 · 审校（C）—— 闭环起点
1. **C1 质检门禁** → 调 `/dbs-ai-check`；`grep` 禁用语式 0 命中；Python 逐题字数 300–500；数字核源。
2. **流程**：硬声明抽取 → grep 核源 → 双源交叉 → 修复 → 两轮 grep；修禁词 / 压字数（不足补自然收尾句不注水）。
3. **产出**：定稿（zh）+ `07-参考资料`（真实来源，单独成章不混正文）。
- **闸门 G3**：正文零信源泄露 + 字数达标，方过闸。
- **螺旋**：未过闸 → 回流 B1 补写/改写，再回 C1。非一次过。

### 阶段 4 · 延展（D）—— 打磨 + 衍生
1. **D1 新料嵌入与修订**：新资料先出「新料嵌入对照表」给你 review；选定后【替换】或【新增+配对删除】；改完回 C1 复检，必要时回 B1。先出表不直接改，被拒即丢，改写仍守契约。
2. **D2 封面钩子与收尾定调**：封面钩子（2 硬核 + 1 有趣，出自原问不加工）；定书名与副线。
- **衍生内容流（成稿后）**：宣传文案（三段式，字数实算，禁破折号，不杜撰）。
- **可选复用层（不阻断主线）**：英文版 100 问 —— 按 `## N` 题号等效英文重写（非机翻），逐题复核；英文零泄露 + 成书语感方收口。
- **闸门 G4**：英文零泄露 + 跨语言成书语感（如做英文版）。

## 7. 契约模板（contract-template.md，A 阶段实例化）
- **零号铁律**：禁杜撰案例与无来源数字。
- **五禁令**：禁破折号 `——` / 禁 `不是…而是…` / 禁正文露信源 / 禁 AI 总结腔（"本质上""更准确地说"）/ 禁模板腔（"值得注意的是"）。
- **字数**：每题 300–500 字（可复制模板不计入）。
- **口吻**：口语风，现象→原因→当下→短判断；长短段交错，不单句成段，禁 6–7 字短句独段。
- **资源**：内联 ≤15 行短资源；长提示词移章末；书末建资源总表；利他≠说明书（先判断后资源）。

## 8. 闸门检查清单（gate-checklist.md）
- **G1**：总纲 + 契约锁定（你拍板）。
- **G2**：100 题齐 + 资料无遗漏。
- **G3**：正文零信源泄露 + 字数达标（grep 0 命中 + Python 实算）。
- **G4**：英文零泄露 + 跨语言成书语感（如做英文版）。

## 9. 参数化（frontmatter 或会话初设）
- `chapters`: N（默认 7，按读者旅程/实践阶梯递进，不按技术模块堆）
- `words_per_q`: [300, 500]
- `body_pattern`: "现象→原因→当下→短判断" | "答疑+交付体四段式"
- `english_layer`: true | false

## 10. 反模式
- 禁止自动过闸（G1–G4 必须你拍板）。
- 禁止杜撰案例 / 无来源数字（零号铁律）。
- 禁止正文露信源、禁词命中后"带病过闸"。
- 禁止把资料搜集 / git 提交纳入本流水线（前置假设 / 另行处理）。

## 11. 安装后文件结构
```
~/.workbuddy/skills/100q-write/
├── SKILL.md                  # 本设计稿 §2–§10
├── contract-template.md      # §7 契约模板
├── gate-checklist.md         # §8 闸门清单
└── references/
    └── pipeline-overview.md  # 四阶段+螺旋全图（即 100问系列-内容创作流水线.md）
```
