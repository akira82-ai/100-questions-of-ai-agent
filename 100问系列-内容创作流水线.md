# 100 问系列 · 内容创作流水线（可复用骨架 v1）

> 合并自三套历史流程描述（vibe-coding / GE / FDE / lark-cli 与 Obsidian 100 问实践）。
> 已剔除「资料搜集」与「git 提交」：纯内容创作主线 = **4 阶段 + 审校闭环 + 衍生内容流**。

## 0. 边界与输入
- **不含**：资料搜集（前置为「已评估资料池」假设，只决定"写什么有依据"）、git 提交/推送。
- **输入**：读者认知 + 已评估资料池 + 工具链（见 §5）。
- **主线**：规划 → 撰写 → 审校 → 延展。
- **性质**：写—校—改是螺旋闭环，非一次性直线；D 阶段新料会回头触发 B/C。

## 1. 四阶段主线

### 阶段 1 · 规划定位（= A 定位）—— 动笔前
| 节点 | 做的事 | 工具 |
|---|---|---|
| A1 用户画像与需求分析 | 定目标读者与痛点/痒点/卡点；定出圈双引擎（向往+恐惧） | /dbs |
| A2 选题定调与模式选择 | 定 mode B（钩子书）；定全书暗线与出圈定位 | /airay-100-questions |
| A3 写 100 问总纲 | 拆 100 问落 N 章 → `00-100问总纲.md`（题号+钩子写法） | — |
| A4 写作方向诊断 | 定"答疑+交付体"四段式（钩子→认知→利他动作→资源块） | /dbs-content |

- **产出**：`00-总纲` + 写作契约。
- **闸门 G1**：总纲与契约锁定，方可开写。
- **硬约束**：画像具体可落地（不写泛泛"技术人"）；出圈靠情绪拉推；方向定调不轻易推翻；结构/模板/引用不碰。

**写作契约（硬规则，全书贯穿）**
- **零号铁律**：禁杜撰案例与无来源数字。
- **五禁令**：禁破折号 `——` / 禁 `不是…而是…` / 禁正文露信源 / 禁 AI 总结腔（"本质上""更准确地说"）/ 禁模板腔（"值得注意的是"）。
- **字数**：每题 300–500 字（可复制模板不计入）。
- **口吻**：口语风，现象→原因→当下→短判断；长短段交错，不单句成段，禁 6–7 字短句独段。
- **资源**：内联 ≤15 行短资源；长提示词移章末；书末建资源总表；利他≠说明书（先判断后资源）。

### 阶段 2 · 撰写（= B 动笔）—— 核心生产
| 节点 | 做的事 |
|---|---|
| B1 逐章逐题成稿 | 按总纲写 100 问；口语风；把评估过的资料**顺译**集成进对应题；可复制模板用 `reference` 格式内联 |
| B2 章首引子与去编辑腔 | 每章开头写引子钩子段；自然段落流，去段标；不写结尾总结句 |

- **产出**：100 问草稿（zh）。
- **闸门 G2**：100 题齐 + 资料无遗漏，方可审校。
- **硬约束**：题号全书连续 1–100；H1 带序号；章首引子绝不用编辑腔标签；改写仍守契约。

### 阶段 3 · 审校（= C 校验）—— 闭环起点
- **C1 质检门禁**：/dbs-ai-check；`grep` 禁用语式 0 命中；Python 逐题字数 300–500；数字核源。
- **流程**：硬声明抽取 → grep 核源 → 双源交叉 → 修复 → 两轮 grep；修禁词 / 压字数（不足补自然收尾句不注水）。
- **产出**：定稿（zh）+ `07-参考资料`（真实来源，单独成章不混正文）。
- **闸门 G3**：正文零信源泄露 + 字数达标，方过闸。
- **闭环**：未过闸 → 回流 B1 补写/改写（螺旋，非一次过）。

### 阶段 4 · 延展（= D 迭代）—— 打磨 + 衍生
| 节点 | 做的事 |
|---|---|
| D1 新料嵌入与修订 | 新资料先出「新料嵌入对照表」给你 review；选定后【替换】或【新增+配对删除】；改完回 C1 复检，必要时回 B1 |
| D2 封面钩子与收尾定调 | 封面钩子（2 硬核 + 1 有趣，出自原问不加工）；定书名与副线 |

- **衍生内容流（成稿后）**：宣传文案（三段式，字数实算，禁破折号，不杜撰）。
- **可选复用层（不阻断主线）**：英文版 100 问 —— 按 `## N` 题号等效英文重写（非机翻），逐题复核；英文零泄露 + 成书语感方收口。
- **闸门 G4**：英文零泄露 + 跨语言成书语感（如做英文版）。

## 2. 贯穿规则（全书生效）
- **零号铁律**：禁杜撰案例与无来源数字。
- **确认闭环**：改正文前先让你拍板；D1 先出表不直接改，只应用你选定的，被拒即丢。

## 3. 闭环与闸门总览
```
A定位 ──G1──▶ B动笔 ──G2──▶ C校验 ──G3──▶ D迭代 ──G4──▶ 收口
                 ▲  ◀──────── 螺旋回炉（未过闸） ───────┘
                 │              D1 新料 → 回 C1 复检 → 必要时回 B1
```
- G1 锁结构 / G3 锁零号铁律与字数 / G4 锁跨语言零泄露。

## 4. 复用注意事项
- **章数 N 因书而异**（vibe-coding/GE/FDE/Obsidian 用 7 章，lark-cli 等用 6 章）：按读者心智旅程/实践阶梯递进，不按技术模块堆。
- **资料搜集前置为假设**：不计入本流，只决定"写什么有依据"。
- **英文版可选**：中文定稿是 1.0，英文是第 2 条内容生产线（等效表达），不阻断主线。
- **契约细节可微调**（如 Obsidian 用「答疑+交付体」四段式，早期书用「现象→原因→当下→短判断」），但零号铁律 + 五禁令为硬底线，不可破。
- **可复制模板**：成稿阶段内联（reference 格式），标记固定，不计入字数；延展阶段统一核对。

## 5. 工具映射（可复用）
| 节点 | 工具 |
|---|---|
| A1 | /dbs（用户画像与需求分析） |
| A2 | /airay-100-questions（mode B 钩子书） |
| A4 | /dbs-content（写作方向诊断） |
| C1 | /dbs-ai-check + grep 禁用语式 + Python 逐题字数 + 数字核源 |
| 撰写集资料 | 已评估资料池（顺译集成） |
| 英文版 | 按 `## N` 等效重写 + 逐题复核（人工/审） |
