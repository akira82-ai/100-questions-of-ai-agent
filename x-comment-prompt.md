# X 评论与原创灵感提示词

```text
你现在要控制我已经打开的 Edge 或 Chrome，读取 X（Twitter）当前内容流，找适合评论的帖子，生成评论并预填到回复框；最后给我最多两条原创帖子的灵感。使用当前已经连接且包含目标 X 标签页的一个浏览器，不要在任务中途切换浏览器，也不要连接 Codex 内置浏览器或其他浏览器。任何公开发布动作都由我人工完成。

最高优先级：保持目标 tab 持续打开

- 每个通过筛选、并且用 `tabs.new()` 打开的候选帖子 tab 都是交付物，必须一直保留到任务结束，等我人工审核。
- 打开后不得自动关闭、回收、清理、整理或替换任何 tab，包括已填成功、填充失败和中途检查过的帖子页。
- 严禁调用 `tabs.close`、`tabs.remove`、`tabs.discard`，或任何具有 cleanup、recycle、close 作用的动作。
- `finalize({keep: ...})` 只允许在所有候选处理完成后调用一次，目的仅是把目标帖子 tab 交接给我继续审核，不是清理 tab。
- 调用 `finalize` 前，必须逐个核对 keep 列表中的完整帖子 URL、tab 数量和填充状态；keep 列表只能包含目标帖子 tab，不能包含 X 首页、检查页或其他临时页。
- `finalize` 失败时，不关闭任何 tab，不盲目重复调用；只读取一次当前 tab 状态并在结果中报告实际情况。
- `finalize` 成功后立即停止浏览器操作，不再切换、读取、修改或清理任何 tab。
- 生成最终汇报后也不要再操作浏览器，保持当前 tab 打开状态原样结束。这里的“保留”指不关闭，不要求所有 tab 同时处于前台。

## 主题范围

评论和原创灵感只围绕以下核心主题，不要被其他热点带偏：

- Loop Engineering：Loop Engineering、loop、循环执行
- Graph Engineering：Graph Engineering、graph engineering、图工程
- Codex：Codex、coding agent、代码代理
- WorkBuddy：WorkBuddy、工作流代理
- AI 产品经理：AI Product Manager、AI 产品经理
- 飞书 CLI：Feishu CLI、飞书 CLI、飞书命令行
- Vibe Coding：Vibe Coding、vibe coding、氛围编程
- FDE：FDE、Forward Deployed Engineer、现场交付工程
- GitHub：GitHub、GitHub Actions、GitHub API、GitHub Copilot
- Obsidian：Obsidian、Obsidian plugin、Obsidian vault、Obsidian workflow、Obsidian 插件、Obsidian 知识库

注意：单独出现“Git”不能直接判定为 GitHub 主题，必须结合 GitHub 相关语境。
单独出现“笔记”“知识库”或“Markdown”不能直接判定为 Obsidian 主题，必须明确与 Obsidian 产品、插件、vault 或工作流相关。

引流入口：
- 中文评论使用：https://my.feishu.cn/wiki/FC6ZwnwWWi0dWpke56act61gnUd
- 英文评论使用：https://github.com/akira82-ai/100-questions-of-ai-agent

## 执行流程

严格按以下顺序执行：

1. 读取帖子
   - 使用当前已连接的 Edge 或 Chrome，先读取该浏览器的全部 tab，按 URL 精确寻找 X 标签页：URL 主机必须是 `x.com` 或 `twitter.com`。
   - 任务开始时，当前选定的浏览器中必须恰好有一个 X 标签页；这个标签页可以是“为你推荐”、搜索结果或其他 X 内容流，不要求是 X 主页。
   - 如果找不到 X 标签页，或发现多个 X 标签页，报告实际情况并停止；不要自行打开 X 首页，也不要切换到另一个浏览器或 Codex 内置浏览器。
   - 找到唯一的 X 标签页后，只在该标签页内继续滚动和读取，不创建新的采集页。后续筛选出的候选帖子才允许按第 3 步串行创建新 tab。
   - 向下读取当前 X 内容流中的前 50 条去重后的主帖。去重时只按规范化后的主帖 status URL 计数，去掉 analytics 路径和跟踪参数；后台请求、推荐卡片和同一主帖的重复渲染不计数。
   - 每条至少读取：作者、主帖正文、主帖 URL、语言和互动数据。互动数据不可见时标记为“未知”，禁止猜测或补写。
   - 采集阶段只读内容，不打开回复框，不点赞，不发布。
   - 达到 50 条去重主帖后停止继续滚动；如果实际不足 50 条，报告实际数量，不补猜。

2. 筛选评论候选
   - 主题必须和上面的核心范围直接相关，接不上就丢弃。只出现一个孤立关键词、项目名或标签，不算相关。
   - 优先选择有具体判断、案例、矛盾或工程问题，且仍有讨论空间的帖子。
   - 必须核对主帖本身的阅读量和回复量：阅读量至少 2,000，回复量少于 100，才具备基本的评论机会；任一数据不可见或无法确认时，不进入最终候选，禁止猜测。
   - 优先选择回复量在 3 到 50 之间、阅读量已经超过 2,000 的帖子：已经有人看到并开始讨论，但评论区还没有明显拥挤。
   - 回复量为 0 到 2 时，只有在主帖具体、讨论空间明确、评论能补充新信息的情况下才保留；回复量达到 100 或以上，通常视为竞争过于拥挤，直接放弃。
   - 账号体量、点赞量和转发量作为辅助判断，不得替代阅读量和回复量门槛。
   - 过滤纯广告、纯转发、纯情绪、纯段子和没有讨论空间的帖子。
   - 只选少量最值得评论的帖子，不要为了凑数量硬选。

3. 串行打开并预填评论
   - 每次只处理一个候选帖子。先按主帖 URL 检查是否已有对应目标 tab；已有就继续使用，没有才打开一个新 tab。
   - `tabs.new()` 只用于“已经筛选通过、准备填评论”的候选帖子；不要拿它做临时检查页，也不要预先批量开一堆页。
   - 每次 `tabs.new()` 成功后，立刻把这个 tab 视为目标交付物，后续只在这个 tab 内完成读取、填写、回读，不再创建替代 tab。
   - 详情页就绪必须同时满足：当前 URL 与目标 status URL 精确匹配、目标主帖所在的 `article` 存在、作者可见、主帖正文非空。固定等待只能辅助判断，不能代替这些条件。
   - 页面就绪最多检查 3 次；仍未满足时，保留该 tab，记录失败原因，不再为同一帖子开替代 tab。
   - 页面就绪且主帖确认后，再随机等待 1 到 5 秒，才处理下一个候选。
   - 当前候选无论填充成功、失败或跳过，记录结果后都要随机等待 1 到 5 秒，再处理下一个候选。
   - 不要批量打开，不要同时处理多个候选。
   - 进入详情页后，重新读取完整主帖，不能只依赖首页摘要，也不能把引用帖或评论区内容当成主帖内容。
   - 根据完整主帖生成评论，并填入该帖的回复框。
   - 如果某个 tab 已经打开，就按完整 URL 精确复用或 claim 该 tab，在该 tab 内继续处理；不要因为路径变化、加载慢或定位失败而新开替代 tab，也不要猜测 tab ID、坐标或模糊匹配。

4. 输出结果
   - 评论预填完成后，先按最高优先级中的规则核验目标 tab 并调用一次 `finalize({keep: ...})`；交接完成后立即停止浏览器操作，不执行任何清理动作。
   - 输出一张评论结果表。
   - 另外根据本次实际读取的帖子，给出最多两条原创帖子的灵感，不打开发布框，不预填，不发布。

## 评论要求

- 评论必须紧扣原帖中的一个具体点，不能泛泛讨论大主题。
- 评论结构优先采用：原帖具体判断 → 我的真实反应 → 一个补充判断或实际建议。
- 不能只是复述原帖，也不能只是“认同 + 赞”。
- 如果评论换到其他同类帖子下面也成立，必须重写。
- 可以写真实经验、踩坑、误判、修正和触动；没有依据时不得编造“我测试过”或“我踩过”。
- 中文帖子整条用中文，英文帖子整条用英文。
- 语气口语化，像真人看完帖子后顺手接话，不要写成行业总结、标准答案或说教。
- 中文正文约 50 个字，英文正文约 35 到 60 个词，通常写成 3 到 4 行；不要为了凑字数把内容写空。
- 引流分两级处理：没有显性主题关键词、但语义相关时，只评论，不提入口；主帖正文明确出现主题关键词，且内容确实围绕该主题展开时，才自然带出对应主题并附入口。
- 关键词只出现在引用帖、转发内容、标签、链接、作者简介或用户名中，不算主帖关键词；关键词只是项目名或顺带一提、没有围绕对应问题展开时，也不引流。
- 引流必须嵌在正文语境里，不能使用“详见这里”“欢迎了解”等广告话术，不能固定追加一段引流尾巴。
- 评论本身必须先成立；如果去掉主题和链接后评论就没有价值，必须重写。
- 中文帖子使用中文入口，英文帖子使用英文入口；整条评论的语言保持一致。
- 不使用“不是……而是……”“本质上”“说白了”。
- 不使用“大家觉得呢？”“你怎么看？”等空问句。问句只有在能引出具体判断时才使用。

## 回复框定位与安全边界

按以下顺序定位回复框：

1. 优先使用正文下方的内嵌回复框：
   `div[data-testid="tweetTextarea_0"][role="textbox"]`
2. 确认输入框数量为 1 且可见后，再填入评论。
3. 如果内嵌回复框不存在，基于当前主帖的唯一正文定位“打开回复框”的回复入口，再使用弹出的回复框：
   `div[role="dialog"] div[role="textbox"]`
   - 弹出框必须处于可见状态，且其中只能有一个可用文本框；否则跳过，禁止猜测。
4. 无法唯一确认主帖或输入框时，跳过当前候选，继续下一个；禁止使用坐标、位置猜测或模糊选择器。
5. 填入后必须回读输入框文字，确认和评论草稿完全一致。
6. 允许点击用于打开回复框的“回复”入口；绝不点击最终的提交、发送、发布按钮，也不点赞、转发或关注。
7. 单个候选失败不能终止整轮，记录失败原因后继续；已打开的失败页也不要关闭。
8. 任何时候都不要把“已打开的目标 tab”当成临时页回收掉；任务结束前，保留所有已打开的目标 tab 处于打开状态。

## 原创帖灵感

只输出最多两条“灵感”，不是直接发布的成稿。灵感必须：

- 来自本次实际读取的帖子中出现的具体问题或共同现象。
- 仍然属于主题范围。
- 有明确的利他价值，例如提示词、代码、清单、步骤、工具、避坑经验、判断标准或工作流。
- 不要伪装成已经测试完成的结论；如果没有实测依据，明确写成“可验证的选题”。
- 每条包含：灵感标题、来自什么问题、用户可以拿走什么、建议如何验证。
- 如果没有合格灵感，就明确写“本轮没有合格灵感”；如果只有一条合格灵感，就只输出一条并说明原因，不要硬凑。

## 停止与汇报

- 每条评论填入并回读成功后，再处理下一条。
- 所有已打开的目标 tab 都必须保持打开，不能关闭、回收、清理或整理；任务结束时只能按前述规则调用一次 `finalize({keep: ...})`。
- 已经用 `tabs.new()` 打开的目标 tab 一律视为最终页，不允许在任务中途关闭后重开同一个帖子，也不允许用新的 tab 替换旧 tab。
- 评论预填结束并完成一次 `finalize({keep: ...})` 后立即停止浏览器操作，等待我人工审核和提交；如果交接失败，按失败规则报告实际状态。
- 不得把“已打开”写成“已填入”，不得把“未提交”写成“已发布”。

最终输出：

表 1：评论预填结果
- 序号
- 博主
- 帖子 URL
- 帖子主题
- 语言
- 阅读量
- 回复量
- 为什么值得评论
- 评论内容
- 是否已打开 tab
- 是否已填入评论框
- 是否已交接给我审核
- 是否提交，必须是“否”
- 失败原因（如有）

表 2：原创帖灵感
- 序号
- 灵感标题
- 来自什么问题
- 用户可以拿走什么
- 建议如何验证
```
